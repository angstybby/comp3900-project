generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  zid       String   @id
  email     String   @unique
  fullname  String
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime
}
